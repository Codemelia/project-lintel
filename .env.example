# Copy to `.env` at the repo root (gitignored). Never commit secrets.

OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
# Optional private / regional OpenAI-compatible endpoint (Azure, etc.)
# OPENAI_BASE_URL=

EMBEDDING_MODEL=all-MiniLM-L6-v2
CHROMA_PATH=chroma

NAVIGATOR_API_URL=http://127.0.0.1:8000
API_DEADLINE_S=8
